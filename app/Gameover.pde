void drawGameOver() {
  background(0);
  fill(255, 0, 0);
  textAlign(CENTER, CENTER);
  textSize(32);
  text("ゲームオーバー", width / 2, height / 2);
  textSize(20);
  text("Enterキーで再入学",width/2, height/2 + 40);
}
